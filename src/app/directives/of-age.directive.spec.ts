import { OfAgeDirective } from './of-age.directive';

describe('OfAgeDirective', () => {
  it('should create an instance', () => {
    const directive = new OfAgeDirective();
    expect(directive).toBeTruthy();
  });
});
